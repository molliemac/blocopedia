<!DOCTYPE html>
<html>
  <head>
    <title>Blocipedia</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <ul class="nav nav-tabs">
        <li> <%= link_to "Blocipedia", welcome_index_path %> </li>
        <li> <%= link_to "Wikis", wikis_path %> </li>

      <div class="pull-right user-info">
        <% if user_signed_in? %>
        <div class="nav-log-in">
            Account: <%= current_user.role %> |
            Signed in as <strong><%= link_to current_user.name, user_session_path %> </strong>|

          <%= link_to "Edit profile", edit_user_registration_path, class: 'navbar-link' %> |
          <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          <% if current_user.role == 'premium'%>
        <h6>Downgrade account <%= link_to "here", users_down_grade_path %></h6>

    <% elsif current_user.role == 'standard'%>
    <h4> Upgrade account <%= link_to "here", new_charge_path %> </h4>
    <% end %>

        </div>
        <% else %>
        <div class="nav-log-in">
          <%= link_to "Sign Up", new_user_registration_path %>  |
          <%= link_to "Log In", new_user_session_path %>
        </div>
        <% end %>
      </div>
    </ul>

      <%= yield %>
    </div>
  </body>
</html>